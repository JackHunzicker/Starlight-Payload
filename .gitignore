# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnpm-store

# Testing
/coverage

# Next.js
.next/
out/
build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
ts-debug.log*
*.log

# Local Env Files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env
*.env.local

# Turborepo
.turbo

# Agent Workspace - Secrets & Logs

# Rendered runtime configs — templates (*.template) are the tracked truth,
# rendered outputs carry secrets. Re-render via deploy/render-configs.*
apps/sharkey/.config/default.yml

# Keep these tracked

# Generated Secrets
.env.generated

# design-sync (generated/machine state — durable set under .design-sync/ IS committed)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
apps/web/.ds-tokens/

# DB dumps — never commit

# Per-machine tool state (not shared)

# Tina generated output — regenerated by `tinacms build`/`tinacms dev` (keep tina-lock.json)
apps/starlight/tina/__generated__/

# Local reference app (packaged Electron build, >100MB files) - never commit

# Nested reference git repos (accidental gitlinks, no .gitmodules) - local-only
